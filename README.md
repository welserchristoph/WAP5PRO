START-Setup:

1. npm install

2. .env Datei erstellen (mit eigener MONGODB_CONNECTION_STRING).

3. Wichtig: "npm run seed", um die Kamera-Daten in Ihre Datenbank zu laden.

4. Starten mit npm run dev und npx nodemon app.js
